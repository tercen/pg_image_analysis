function strID = fromFile(strFileName, strRefMarker)
global mask
global xOffset
global yOffset
global spotPitch
global spotSize
global rotation
global oArray

[oArray, strID] = fromFile(oArray, strFileName, strRefMarker);
% EOF